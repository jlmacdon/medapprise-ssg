import * as React from "react";
import Layout from "../components/Layout";

const NotFoundPage = () => (
  <Layout>
    <div>
      <h1>NOT ALL THOSE WHO WANDER ARE LOST.</h1>
      <p>But you seem to have lost your way.</p>
    </div>
  </Layout>
);

export default NotFoundPage;
